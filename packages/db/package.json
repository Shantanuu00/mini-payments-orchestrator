{
  "name": "@pkg/db",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/schema.test.mjs",
    "db:apply": "node ./apply-schema.js",
    "db:migrate": "node ./migrate.js up",
    "db:rollback": "node ./migrate.js down"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/pg": "^8.11.10",
    "typescript": "^5.8.2"
  },
  "exports": "./src/index.ts"
}
